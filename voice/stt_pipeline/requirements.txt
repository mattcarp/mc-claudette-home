fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
# Whisper backend — install at least one:
# Option A (preferred — lower latency, int8 quantisation):
faster-whisper>=1.0.3
# Option B (fallback — already on Workshop as of 2026-03-10):
# openai-whisper>=20250625
